query ShareItem($itemId: ID!) {
  shareItem(id: $itemId) {
    id
    date
    item {
      id
      title
      kind
      itemDetails {
        id
        title
        kind
        place
        url
        memo
        priority
      }
    }
  }
}
